package com.example.expensetracker.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * High-Impact Bluish Palette
 * Optimized for readability and contrast in both Light and Dark themes.
 */

// Core Brand Colors
val PrimaryBlue = Color(0xFF005AC1)
val SecondaryBlue = Color(0xFF535F70)
val TertiaryBlue = Color(0xFF0061A4)

// High-Impact Functional Colors
val IncomeGreen = Color(0xFF22C55E) // Brighter Green
val ExpenseRed = Color(0xFFEF4444)  // Brighter Red
val SavingsBlue = Color(0xFF3B82F6) // Brighter Blue

// Light Theme Neutrals
val LightBackground = Color(0xFFFDFBFF)
val LightSurface = Color(0xFFEBEBF3)
val LightOnSurface = Color(0xFF1A1C1E)
val LightSurfaceVariant = Color(0xFFE0E2EC)
val LightOnSurfaceVariant = Color(0xFF44474E)

// High-Impact Dark Theme Neutrals
val DarkBackground = Color(0xFF0B0E14) // Deeper Black-Blue
val DarkSurface = Color(0xFF151921)    // Distinct Surface
val DarkOnSurface = Color(0xFFF8FAFC)  // High-Contrast White
val DarkSurfaceVariant = Color(0xFF2D333F) // Lighter Gray for containers
val DarkOnSurfaceVariant = Color(0xFFCBD5E1)

// Legacy compatibility aliases
val PrimaryModern = PrimaryBlue
val SecondaryModern = SecondaryBlue
val TertiaryModern = TertiaryBlue
val Purple40 = PrimaryBlue
val PurpleGrey40 = SecondaryBlue
val Pink40 = TertiaryBlue
val Purple80 = Color(0xFFADC6FF)
val PurpleGrey80 = Color(0xFFBBC7DB)
val Pink80 = Color(0xFFD1E4FF)
